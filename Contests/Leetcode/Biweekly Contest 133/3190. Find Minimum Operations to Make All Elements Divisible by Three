class Solution {
    public int minimumOperations(int[] nums) {
        int ans=0;

        for(int x: nums)
        {

           if(x%3==1) ans++;
           else if(x%3==2) ans++;
           
        }
        return ans;
    }
}
