class Solution {
    public int findUnique(int[] arr) {
        // code here
        int sum=0;
        for(int r=0;r<arr.length;r++)
        {
            sum=sum^arr[r];
            
        }
        return sum;
    }
}
